import * as React from 'react';

export interface HoldProps {
}

export default class Hold extends React.Component<HoldProps, any> {
    public render() {
        return (
            <div>
                hold
      </div>
        );
    }
}
